# Model Config Fixtures

The configs in this folder are software-test and example fixtures unless a file
explicitly declares curated scientific provenance.

Files named `toy_*` exercise configured-model plumbing only. Files named
`synthetic_*` support synthetic-only calibration or comparison tests. Do not
cite outputs from these configs as biological evidence.
