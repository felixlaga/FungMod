# FungMod pinned runtime environment lock.
#
# Exact resolved versions of FungMod's runtime dependency closure.
# Generated with: python -m pip install . && python -m pip freeze
# Interpreter:   CPython 3.13 on macOS (arm64).
#
# Reproduce the exact environment (from a clean venv):
#     python -m pip install -r requirements-lock.txt
#     python -m pip install . --no-deps
#
# For an exactly pinned toolchain including dev/docs/notebook extras, add
# those extras in a fresh environment and regenerate. For a fully isolated
# environment, use the provided Dockerfile instead.
#
# NumPy/SciPy pins reflect the 3.13 resolution; on 3.11/3.12 pip will pick
# the nearest compatible wheels. Regenerate on your target interpreter if
# you need bit-for-bit dependency identity there.
cffi==2.1.0
contourpy==1.3.3
cryptography==50.0.0
cycler==0.12.1
flexcache==0.3
flexparser==0.4
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.1
numpy==2.5.1
packaging==26.2
pillow==12.3.0
Pint==0.25.3
platformdirs==4.11.0
pycparser==3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3
scipy==1.18.0
six==1.17.0
typing_extensions==4.16.0
